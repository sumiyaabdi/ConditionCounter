% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotTrend.R
\name{plotTrend}
\alias{plotTrend}
\title{Plot condition occurrence trends}
\usage{
plotTrend(data, byMonth = FALSE, conditionConceptId = NULL)
}
\arguments{
\item{byMonth}{Logical. If TRUE, plot by year_month; otherwise by year.}

\item{conditionConceptId}{Optional. One or more concept IDs to filter.}

\item{extractedCounts}{A data.frame with
\code{condition_concept_id}, \code{year}, \code{month}, \code{n_patients}.}
}
\value{
A ggplot object.
}
\description{
Takes output from the \code{extractPatients()} function as input and returns a plot with the number of patients per year (default) or per month for each condition in "data".
Internally calls \code{prepareTrendData()} and \code{plotPreparedData()}.
}
\examples{
df <- data.frame(
  condition_concept_id = c(10, 10, 10, 20, 20, 20),
  year  = c(2020, 2020, 2021, 2020, 2021, 2021),
  month = c(1, 2, 1, 3, 1, 2),
  n_patients = c(5, 8, 3, 10, 4, 7)
)

# Prepare monthly data
plotTrend(df, byMonth = TRUE)

# Prepare yearly aggregated data
plotTrend(df, byMonth = FALSE)

# Filter to a single concept
plotTrend(df, conditionConceptId = 10)
}
